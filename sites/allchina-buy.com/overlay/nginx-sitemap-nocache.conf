# spam-update-p0
# Do not add extra `location = /robots.txt` blocks — the vhost already has them.
# Patch the existing Cache-Control from public max-age=3600 to:
#   add_header Cache-Control "no-cache, must-revalidate" always;
