# spam-update-p0: keep /product/ crawlable so Google can see noindex,
# but do not treat the cloned SKU pages as indexable.
location ^~ /product/ {
    add_header X-Robots-Tag "noindex, follow" always;
    try_files $uri $uri/ $uri/index.html =404;
}
